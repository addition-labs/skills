# Origin

This skill folder is a verbatim copy of `(repository root)` from
https://github.com/leopard627/fire-your-seo-agency at commit `eb9be9f`, by leopard627, licensed MIT
(see LICENSE in this folder). The only additions are this file and the LICENSE copy.

Upstream this skill is the whole repository; its README and repository files are not copied.

Modified: the frontmatter `name` field reads `five-lane-seo` so it matches this folder, as the Agent Skills specification requires. Nothing else in the file changed.

Not copied: the repository's README files, `.claude-plugin/` manifest and `assets/social-preview.png`; none is part of the skill.
